Implement a feature that reads user credentials from a local JSON file during the registration process. Use the registration form from week 6.
Create a users.json file containing an array of user objects with the following properties: name, username, email, password, age, phone, and gender.
Develop a registration.js file containing a function handleRegistration that:
Validates that the username and email are unique.
Display success or failure messages dynamically on the registration page.
